根级 `task dev` 启动 Wails 开发模式（默认端口 9245），`task build` / `task package` 按当前 OS 分发到对应 `build/<os>/Taskfile.yml` 完成交叉编译；`task build:server` / `run:server` 用于无 GUI 的 HTTP 服务模式，配合 `task build:docker` / `run:docker` 进行容器化部署。