Windows 本地构建统一通过 `pwsh scripts/wails/build.ps1` 一键完成；各平台单独构建使用 `./scripts/build-{linux,darwin,android,ios}.sh [--production] [--clean]`。图标 bundle 需联网一次：`cd frontend && node ../scripts/gen-icon-bundle.mjs`，之后可加 `--check` 仅校验。纹理生成需先 `pip install Pillow`。