`npm run generate:bindings` 会回退到仓库根执行 `wails3 generate bindings -ts -i -d frontend/bindings ./...` 生成 Go→TS 类型；MPR（多线程 WASM 物理）需同时设置 `VITE_MMD_WASM_MT=true` 并在 Go 端启用对应中间件才会生效；E2E 通过 `npm run test:e2e:headed` 或 `--ui` 运行 Playwright。