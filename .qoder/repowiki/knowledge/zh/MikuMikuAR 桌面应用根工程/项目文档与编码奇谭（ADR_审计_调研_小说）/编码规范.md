- ADR 文件采用 `adr-NNN[-sub].md` 前缀命名，标题行包含编号与主题，作为唯一权威状态源。
- 审计与 Bug 日志使用日期前缀（如 `2026-07-11-*.md`、`round-N-*.md`）组织，便于按时间线检索。
- Novel 各章末尾维护「章节 | 标题」Markdown 表格索引，新增/迁移时仅更新对应章索引而非全局统计表。
- Novel 章节名与前端源码目录一一对应（如 `02-UI交互/` ↔ `frontend/src/menus/`），通过 `README.md` 顶部映射表建立可追溯关系。