# Build artifacts
build/bin
frontend/dist
frontend/dist-web
/dist/
frontend/node_modules

# E2E
frontend/playwright-report/
frontend/test-results/
frontend/e2e/logs_*/

# Model files — do NOT commit (copyrighted, large)
text-model/

# Plaza 本地缓存（客户端运行时写入用户目录 plaza-cache/ 或 IndexedDB；仓库根仅保留云端源 creators.json + workshop_sites.json）
plaza_cache.json

# Generated locale JSON files (build artifacts from scripts/generate-locale-json.mjs)
frontend/public/locales/

# Dependencies
node_modules/
# 文档站（VitePress，站点根 = docs/）：依赖与构建产物不入库（img/*.png 为构建硬依赖，必须入库）
docs/node_modules/
docs/.vitepress/dist/
docs/.vitepress/dist-verify/
docs/.vitepress/dist-test/
docs/.vitepress/.temp/
docs/.vitepress/cache/
docs/.vitepress/.temp.bak/

# Reasonix internal state
.reasonix/

# Other projects in workspace
MMDDownloader/
mmd_tools_repo/
reasonix.toml

# Environment variables
.env

# OpenOmni runtime state (ephemeral sessions)
.omo/

# CodeBuddy / WorkBuddy — other-software memories and local config
.codebuddy/
.workbuddy/
.mimocode/

# Qoder — other-software generated repo wiki/artifacts (vendor-generated, not ours)
.qoder/

# Test coverage output
coverage/
# Coverage ad-hoc 残留（跑覆盖率时的临时产物）
coverage-fresh/
coverage-tmp/
coverage-run.log
vitest.coverage.tmp.config.ts
# vitest transform/预构建缓存（cacheDir 挪出 node_modules 供 CI 跨 run 复用）
frontend/.vitest-cache/

# Wails dev 运行日志（本地临时产物，不入库）
dev-run*.log
wails3-dev-*.log

# Binary / executable artifacts
*.exe
*.bak
*.test
*.out

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temp
*.tmp
tmp/

# Wails3 build temp
.bindings-tmp-*
.task/
frontend/wailsjs/
bin/
*.syso

# Android build artifacts
build/android/.gradle/
build/android/build/
build/android/gen/
build/android/local.properties
build/android/app/build/
build/android/app/.gradle/
build/android/*.jar
build/android/*.apk

# Android signing keystore (secrets — never commit)
*.keystore
*.jks

# Trae IDE
.trae/

# Runtime local config (absolute paths + personal data — never commit)
setting/
nul
docs/_writetest.txt
frontend/report/jscpd-report.json
frontend/jscpd-report/

# 文档漂移检查 · 推送后 AI 快照（非提交物，由 pre-push hook 生成）
docs/.doc-check-last.json
docs/.doc-check-next-steps.md
_tmp_atomgit_cli/
base_export/
