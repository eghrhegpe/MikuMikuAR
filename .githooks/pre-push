#!/bin/sh
# MikuMikuAR pre-push — 已精简，全部质量门禁移至 CI
# CI 工作流: .github/workflows/ci.yml（push/PR 到 main 时触发）
# 安装方式: git config core.hooksPath .githooks
# 跳过一次: git push --no-verify（不推荐）

echo "[pre-push] 质量检查已移至 CI，推送中..." >&2
