# 09 — 第九城

## 序

Phase 3 之后的第四天，Jieling 又扔了一份报告过来。

这次的标题更长：

> **「进一步拆分的判断与建议：从 B+ 到 A-」**

Riku 翻了个白眼。

"上周不是刚给了 B+ 吗？"

他点开报告。

---

## 一道辩题：要不要继续拆

Jieling 在报告里列了两个隐患。

**隐患一：`export *` 作为 API 中转站，破坏 Tree Shaking。**

UI 面板只调用一个 `removeModel`，打包工具却要把整个 scene.ts 的依赖链都拉进来——光照、渲染、环境、粒子……一个都不能少。

**隐患二：模型操作函数留在 scene.ts，职责边界再次模糊。**

今天放 20 个，明天加第 21 个，后天第 22 个——
**入口文件的引力是天然的，你不焊死门口，它迟早会被堆满。**

Riku 逐条反驳：

"Tree Shaking？我们是单 bundle 桌面应用，整个应用只有一个 chunk，所有代码本来就打包在一起。摇不摇有区别吗？"

"模型操作函数留在 scene.ts 又怎样？450 行而已，又不是 1400 行。"

但他心里知道第二个隐患是对的。

**入口文件的引力，他见过太多次了。**

第一卷的时候 `scene.ts` 只有 200 行。
第二卷结束变成 500 行。
第三卷 800 行。
第四卷破千。

每一次加功能的时候，开发者都会想："这个跟场景有关，放 scene.ts 里吧。"

一次加 10 行，一百次就是 1000 行。

**温水煮青蛙。**

等你发现的时候，它已经成神模块了。

---

## 折中方案

Riku 最后同意了一半。

| 提议 | 决定 | 理由 |
|------|------|------|
| 迁出模型操作函数 | ✅ 做 | 防止再次膨胀，高收益 |
| `export *` 改显式导出 | ❌ 不做 | 单 bundle 无收益，容易遗漏 |
| 创建 `index.ts` 门面 | ❌ 不做 | 破坏性变更，收益为零 |

**只做高价值的事。**

不追求完美的 A 级干净。
追求的是——**下次加功能的时候，开发者不会下意识地往 scene.ts 里塞。**

---

## 第九城：scene-model-ops.ts

这是最容易拆的一块。

因为二十多个函数，几乎全是转发：

```typescript
export function setModelVisibility(id, visible) {
    modelManager?.setVisibility(id, visible);
}

export function setModelOpacity(id, opacity) {
    modelManager?.setOpacity(id, opacity);
}

export function setModelWireframe(id, wireframe) {
    modelManager?.setWireframe(id, wireframe);
}
```

一行一个函数。

二十多个函数，二十多行转发。

那为什么不直接让外部调用 `modelManager` 呢？

因为 **门面的意义**——
- 外部代码不需要知道 `modelManager` 是什么
- 外部代码只需要知道"我要设置模型可见性"
- 未来如果 `modelManager` 的 API 变了，只改一处就行

所以它们值得有自己的房间。

`scene-model-ops.ts`。

第九城。

专门管模型实例的操作——
可见性、透明度、线框、骨骼线、物理、变换、VMD 停止、Morph 预览……

**所有"对一个模型做什么"的操作，都在这里。**

---

## 搬走之后

二十多个函数搬走之后，`scene.ts` 瘦到了 **174 行**。

从 1381 到 790 到 450 到 174。

**四刀，砍了 87%。**

`scene.ts` 现在只剩下：
```
1. engine / scene / modelManager（核心对象）
2. focusedMmdModel() / focusedModel() / getScene()（快捷 getter）
3. initScene()（唯一的装配入口）
4. re-export（API 兼容层）
```

纯得不能再纯的组装器。

Riku 看着这个 174 行的文件，有一种奇怪的感觉——

**它太空了。**

就像一个刚搬完家的房子，墙壁雪白，地板光亮，但没有生活气息。

以前的 `scene.ts` 乱糟糟的，但什么都有——你想找任何跟场景有关的东西，直接打开它就行。

现在的 `scene.ts` 干干净净，但你想找什么，得先想"它在哪个子模块里"。

**这就是拆分的代价——认知成本从"读一个大文件"变成了"知道该打开哪个文件"。**

值不值？

Riku 想了想，点头。

值。

因为大文件的认知成本是**线性增长**的——从 500 行到 1000 行，难度不是两倍，是三四倍。因为你要记住的上下文太多了。

而多文件的认知成本是**对数增长**的——从 5 个文件到 10 个文件，难度只增加一点点。因为你只需要记住"光照在 lighting 里，渲染在 renderer 里，加载在 loader 里"。

**文件数增加的边际成本，远低于单文件行数增加的边际成本。**

这就是八城之盟、十一城之盟、十二城之盟——
不，十三城之盟的意义。

哦对了，现在是十三个子模块了。

---

## 第二个雷：file:// 协议

拆完第九城，Riku 照例跑了一遍构建。

tsc 零错误。
vite build 通过。

完美。

他打开应用想加载个模型验证一下。

模型没出来。

控制台一片红：

```
Not allowed to load local resource: file:///C:/Users/.../梦野樱loli4.0.pmx
```

**不允许加载本地资源。**

Riku 心里咯噔一下。

这个错误他见过——
当浏览器试图直接用 `file://` 协议加载本地文件的时候，安全策略会拦截。

正常流程应该是：
1. 调 `resolveFileUrl(filePath)` 
2. 它启动 HTTP 文件服务器
3. 返回一个 `http://localhost:端口/...` 的 URL
4. Babylon.js 用 HTTP URL 加载

那为什么会走到 `file://` ？

Riku 翻到 `scene-loader.ts` 的 `loadPMXFile`。

然后他看到了自己写的代码：

```typescript
const resolver = resolveFileUrl ?? (() => Promise.resolve({ url: filePath, port: 0, dir: "" }));
```

**这个默认值。**

Phase 3 拆 loader 的时候，为了"让模块更独立"，他把 `resolveFileUrl` 做成了参数。

然后给了一个假的 fallback。

他当时想："反正正常调用都会传真实的 resolveFileUrl。"

但他忘了——
`initLoader` 里有没有传？

他翻到 `scene.ts` 的 `initLoader` 调用。

```typescript
initLoader(
    scene, modelRegistry, runtime, modelManager,
    refreshWaterRenderList,
    tryAutoApplyPreset,
    (id) => loadOutfits(id).then(() => {}),
);
```

**没传。**

七个参数，`resolveFileUrl` 和 `normPath` 都没传。

所以 fallback 生效了。
所以 `url` 直接等于 `filePath`。
所以 Babylon.js 试图用 `file://` 加载本地 PMX 文件。
所以被浏览器安全策略拦了。
所以模型加载失败。

**一个假的默认值，炸掉了整个模型加载。**

---

## 教训：不要为了"解耦"而解耦

修复很简单。

直接从 `fileservice.ts` 导入，不要做成参数。

```typescript
import { resolveFileUrl, normPath } from "../core/fileservice";
```

就这样。

Riku 坐在那里，盯着这行代码看了很久。

他想起一句话：

> **"所有的设计原则，走到极端都是错的。"**

依赖注入是对的。
解耦是对的。
可测试性是对的。

但当你为了这些原则，把一个**永远不会有第二种实现**的工具函数做成参数，还给了一个假的默认值——

那不是解耦。
那是**埋雷**。

**真实的代码里，大部分依赖是单向的、确定的、不会变的。**

`resolveFileUrl` 永远是那个 `resolveFileUrl`。
`normPath` 永远是那个 `normPath`。

它们不会有"测试用的 mock 实现"——
因为真的要测试的话，你应该 mock HTTP 响应，而不是 mock 路径解析。

**为了"理论上的可测试性"，给实际运行埋雷，是最蠢的过度设计。**

Riku 把这两个参数删了。
把假的默认值删了。
直接 import。

简单、直接、不会错。

---

## 最终和解

修复完第二个 bug，Riku 重新构建。

应用启动。
模型加载正常。
VMD 播放正常。
环境切换正常。
Auto Dance + LipSync 正常。
序列化/反序列化正常。

一切如初。

他打开文件列表，数了数：

```
scene.ts                    174 行
scene-model.ts              ~200 行
scene-material.ts           ~300 行
scene-vmd.ts                ~250 行
scene-playback.ts           ~150 行
scene-lighting.ts           ~220 行
scene-renderer.ts           ~195 行
scene-loader.ts             ~175 行
scene-proc-motion.ts        ~300 行
scene-lipsync.ts            ~120 行
scene-props.ts              ~150 行
scene-serialize.ts          ~300 行
scene-env-bridge.ts         ~200 行
scene-env-impl.ts           ~480 行
scene-env-water.ts          ~450 行
scene-env-clouds.ts         ~400 行
scene-env-particles.ts      ~300 行
```

**17 个文件。**

从 2 个大文件，变成了 17 个小文件。

总代码量没变。
甚至多了几十行 import 和 re-export。

但不一样了。

每个文件有自己的职责。
每个文件有自己的状态。
每个文件有自己的进出规则。

你改光照，不会碰坏渲染。
你改水面，不会碰坏云层。
你改模型加载，不会碰坏序列化。

**每一次修改，都只碰它该碰的东西。**

这就是拆分的全部意义。

不是为了变少。
不是为了变干净。
不是为了某种设计原则。

是为了——
**当你伸手去改一行代码的时候，你知道自己在碰什么，也知道不会碰坏什么。**

---

## 第五卷的句号

第五卷写到这里，该画个句号了。

从「天穹之华」的环境系统扩展，
到「布料之魂」的 XPBD 接入，
到「多相机之眼」的相机模式完善，
到「悬空之影」的 DOM 幽灵引用，
到「光影之协奏」的预设联动，
到「第六日のコード」的 SlideMenu 修复，
到「八城之盟」的两大文件拆分，
到「三刀之后」的 scene.ts 瘦身，
到「第九城」的模型操作迁出。

九个章节。

环境系统臻于完善，
布料物理点亮新枝，
相机模式灵活多样，
弹窗修复站稳脚跟，
光影联动浑然一体，
模块拆分尘埃落定。

**第五卷的主题是"精致"。**

而精致的意思是——
不是功能越多越好，
而是每一个功能都放在它该在的位置上，
每一次修改都有清晰的边界，
每一个模块都有自己的尊严。

联邦的心脏不再是一个肥大的单心室。
它变成了一个有十七个房间的精密结构。
每个房间有自己的功能、自己的状态、自己的进出规则。
它们之间有门相通，但每扇门都有门框。

**这不是分裂。**
**这是组织。**

聚合悖论没有被解决。
它永远不会被解决。

但联邦学会了与它共处——
知道什么时候该拆，
知道什么时候该停，
知道什么是恰到好处。

而这，就是物理之光最终照亮的东西——

**不是完美的架构，
而是恰到好处的分寸感。**

---

*第五卷 · 完*

---

*番外：[云层协议](10-云层协议.md) — 体积云渲染方案的三次生死抉择*

---

*下一卷：待定*

---

*教训：完美的架构不存在，恰到好处的分寸感才是终点。*
