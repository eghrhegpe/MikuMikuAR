# 07 — 八城之盟

## 序

议会学会了站立。

第六日的代码在 `offsetHeight` 的强制回流中站稳了脚跟，SlideMenu 不再在空白中抽搐。联邦的界面终于有了一副像样的骨架——卡片化的菜单、统一的滑块、预设芯片、折叠面板，每一个部件都站在它该站的位置上。

但 Riku 知道，骨架之下，内脏正在肿胀。

他打开 `scene.ts` 的文件信息。

1381 行。

不是最多的，但它是联邦的心脏。PMX 加载在这里，VMD 播放在这里，序列化在这里，环境桥接在这里，程序化动作在这里，口型同步在这里，道具系统在这里——所有带血的管子都插在这一个文件上。

然后是 `scene-env-impl.ts`。

1624 行。

环境系统的巨无霸。天空、地面、水、云、粒子、风、雾、时间流转——整个天穹被塞进了一个文件里。

Riku 把两个文件的行数截图发给 Jieling。

> "这两个再涨下去，下一次改 bug 就是在雷区里跳舞。"

Jieling 的回复很简短：

> "拆。"

---

## 史官的账簿

在动手之前，Riku 先做了一件事——列清单。

不是列"要拆什么"，是列"每一样东西在哪里、和谁说话、说什么话"。

`scene.ts` 的人口普查：

| 部门 | 行数 | 依赖谁 | 被谁依赖 |
|------|------|--------|----------|
| Babylon 核心初始化 | ~200 | 引擎、运行时 | 所有人 |
| 模型加载与生命周期 | ~150 | PMX Loader、Physics | library.ts |
| 光照与渲染状态 | ~100 | DirectionalLight、Pipeline | scene-menu.ts |
| 序列化/反序列化 | ~250 | modelRegistry、camera.ts | main.ts |
| 程序化动作 | ~150 | BeatDetector、Audio | scene-menu.ts |
| 口型同步 | ~80 | morph、Audio | scene-menu.ts |
| 道具系统 | ~100 | ImportMeshAsync | — |
| 环境桥接 | ~200 | env-lighting、scene-env.ts | scene.ts 内部 |
| 播放控制 | 已拆出 | — | scene-playback.ts |
| VMD 加载 | 已拆出 | — | scene-vmd.ts |

`scene-env-impl.ts` 的人口普查：

| 部门 | 行数 | 依赖谁 |
|------|------|--------|
| 天空与地面 | ~300 | 渐变纹理、太阳盘 |
| 水面系统 | ~450 | Gerstner Shader、涟漪、焦散 |
| 云层系统 | ~400 | Perlin 噪声、体积云 Shader |
| 粒子系统 | ~300 | GPUParticleSystem、风 |
| 雾与时间流转 | ~150 | Scene.fog、观察者 |

两份清单摆在一起，Riku 看到了同一个问题：

**核心模块像一个过载的电源插排，所有电器都插在上面。**

只要拔错一个，整个屋子就黑了。

---

## 第一刀：scene.ts

拆文件从来不是技术活，是仪式活。

这句话在第四卷「瘦身日」那一章已经被验证过一次。但那一次拆的是模型材质、VMD 加载、播放控制——都是相对独立的肢体。

这一次要拆的是内脏。

**第一刀：程序化动作（Idle / Auto Dance）。**

`scene-proc-motion.ts`。这是最容易识别的一块——它有自己的状态（`procState`、`procBeatDetector`、`procVmdActive`），有自己的生命周期（`startProcMotion`、`stopProcMotion`、`updateProcMotion`），甚至有自己的节拍检测器。

但它不是孤岛。它要知道当前聚焦的模型是谁，要调用 `loadVMDMotion` 把生成的 VMD 绑上去，要在模型被移除时清理自己的痕迹。

「从 scene.ts 搬走的代码，还能访问 scene.ts 里的东西吗？」

这是所有拆分的第一问。

ESM 的回答是：**能，但要讲规矩。**

规矩就是——**模块顶层不调用，函数体内访问。**

```typescript
// ✅ 安全：静态 import，但只在函数里用
import { focusedModel, loadVMDMotion } from "./scene";

export async function updateProcMotion(): Promise<void> {
    const model = focusedModel();  // 函数体内访问，ES module live binding 保证拿到最新值
    if (model) {
        // ...
    }
}
```

这不是什么高深技巧。这是 `scene-vmd.ts` 和 `scene-playback.ts` 早就走过的路——同一个模式，同一种活法。

Riku 把这个模式写进了注释里：

> 「静态 import 安全，模块顶层不调用，函数体内访问，ES module live binding 保证。」

**第二刀：口型同步。**

`scene-lipsync.ts`。比程序化动作更小，更纯粹。它只做一件事——把音频频段能量映射成 morph 权重。

它的依赖更少：`procBeatDetector`（共享节拍检测器）、`focusedModelId`、`setModelMorphWeight`。

搬走的时候 Riku 顺手做了一件事——把 `resetLipMorph` 也搬过去了。这个函数本来就是为 Lipsync 服务的，放在 scene.ts 里像别人家的孩子。

**第三刀：道具系统。**

`scene-props.ts`。这是最"干净"的一块——它用独立的 `propRegistry`，不走 `modelRegistry`，不碰 VMD，不碰物理。

搬走它几乎没有心理负担。唯一需要注意的是：道具需要向 `ShadowGenerator` 注册投射阴影，所以得从 `scene-env.ts` 里拿 `_envSys`。

「道具和环境系统的耦合，比和场景核心还深。」Riku 感慨。

**第四刀：场景序列化。**

`scene-serialize.ts`。这是最胖的一块，也是依赖最复杂的一块。

它要知道：
- 所有模型的状态（位置、缩放、可见性、物理、服装变体）
- 相机状态、光照状态、渲染状态
- 环境状态
- 程序化动作状态、口型同步状态
- 道具列表
- 重力强度
- 音频状态
- 相机 VMD 状态

……基本上联邦里每一个部门的家底它都要摸一遍。

搬走它的代价是：`scene-serialize.ts` 的 import 列表长得像一份联邦百官名册。

```typescript
import { loadCameraVmdFromPath } from "./scene-vmd";
import { removeProp, loadProp, setPropTransform } from "./scene-props";
import { setEnvState } from "./scene-env-bridge";
import { setGravityStrength, getGravityStrength } from "./scene-env-bridge";
import { regenerateProcMotion, getProcMotionState, setProcMotionState } from "./scene-proc-motion";
import { getLipSyncState, setLipSyncState } from "./scene-lipsync";
// ... 还有很多
```

Riku 盯着这一串 import 看了半分钟。

「这会不会循环依赖？」

他在脑子里画了一张图：

```
scene-serialize.ts → scene.ts → scene-serialize.ts
```

是的，循环了。

但 ESM 的规则是——**只要你不在模块顶层调用对方，循环就不会触发。**

`deserializeScene` 是一个函数。用户点"加载场景"的时候才会调用它。那时候所有模块都已经初始化完毕，所有导出都已经挂好，循环只是地图上的一条线，不是运行时的死锁。

Riku 在文件顶部加了一行注释：

> 「依赖较多，但所有导入对象仅在函数体内使用，运行时循环依赖不触发。」

像是给自己的备忘，也像是给下一个 AI 的警示牌。

**第五刀：环境桥接。**

`scene-env-bridge.ts`。这是最微妙的一块——它不是环境系统本身，是环境系统与场景核心之间的翻译官。

`envAutoLink`（天空色推导光照）在这里，`sunAngle`（太阳角度）在这里，`timeOfDay`（时间流转）在这里，`applyEnvPreset`（环境预设）在这里，`setEnvState`（统一入口）在这里，`gravity`（重力控制）在这里。

它们的共同特征是：**一只脚在场景里，一只脚在环境里。**

把它们放在 scene.ts 里，scene.ts 就会被环境系统的细节污染。
把它们放在 scene-env-impl.ts 里，impl 就会被场景核心的细节污染。

所以它们值得有自己的房间。

---

## 第二刀：scene-env-impl.ts

环境系统的拆分，比 scene.ts 更有诗意。

因为每一块都是一个完整的世界——水面是一个世界，云是一个世界，粒子是一个世界。它们共享同一个天空，但各有各的法则。

**第一城：水面。**

`scene-env-water.ts`。

Gerstner 波浪 Shader 在这里，涟漪系统在这里，焦散纹理在这里，水下过渡在这里。

搬走它的时候遇到了一个小问题：水下过渡的每帧更新逻辑，原来写在 `ensureEnvUpdateObserver` 里——也就是环境系统的统一观察者里。

如果把水下逻辑整个搬走，那 observer 怎么办？

Riku 想了两个方案：

方案 A：把 observer 也搬到 water.ts 里。
方案 B：water.ts 提供一个 `updateUnderwaterTransition(scene, pipeline)` 函数，observer 还是在 impl 里，调用它。

方案 A 更干净，但方案 B 更安全——因为 observer 里还管着云层漂移、天空旋转、粒子更新。把它们拆散到多个文件，就会有多个独立的 scene observer，每帧都要走一遍注册、触发、注销。

「一个观察者，多个回调。」Riku 选了 B。

于是 `scene-env-water.ts` 导出了 `updateUnderwaterTransition` 和 `resetUnderwaterState`，impl 的 observer 每帧调用它们。

**这是依赖反转——高层模块（impl 的 observer）定义接口，低层模块（water）实现细节。**

Riku 自己都没意识到他做了一个架构模式的选择。他只是觉得"这样更稳"。

**第二城：云层。**

`scene-env-clouds.ts`。

Perlin 噪声函数在这里，体积云 Shader 在这里，`createClouds` 和 `disposeClouds` 在这里。

这是最独立的一块——它几乎只依赖 `_envSys.clouds` 状态和 `getScene()`。搬走它像从书架上抽出一本书，书脊上的灰掉了一地，但书架本身纹丝不动。

**第三城：粒子。**

`scene-env-particles.ts`。

粒子纹理生成在这里，`createParticleEmitter` 和 `disposeParticles` 在这里，风对粒子的作用在这里。

和云层一样干净。

---

## 统一观察者

三个子模块都搬走之后，impl.ts 里剩下的东西变少了。

但 Riku 注意到一件事——`scene-env-bridge.ts` 里的时间流转（Time-of-Day），用的是自己独立的 `scene.onBeforeRenderObservable` 观察者。

而 impl.ts 里也有一个 `_envUpdateObserver`。

两个观察者，都在 `onBeforeRender` 上挂着。每帧各走各的，互不干扰，但也互不知情。

「为什么不能只有一个？」

Riku 在 impl.ts 里加了一个回调注册表：

```typescript
const _sceneTickCallbacks = new Set<() => void>();

export function registerSceneTickCallback(cb: () => void): () => void {
    _sceneTickCallbacks.add(cb);
    return () => _sceneTickCallbacks.delete(cb);
}
```

然后在 observer 的最后加了一行：

```typescript
for (const cb of _sceneTickCallbacks) cb();
```

bridge.ts 的时间流转，不再自己挂 observer 了。它调用 `registerSceneTickCallback(_timeOfDayTick)`，把自己的 tick 函数注册进去。

**从此联邦只有一个心脏跳动——env 的 `_envUpdateObserver`。云层、水面、粒子、时间流转，都在同一次心跳里被推动。**

这不是性能优化。每帧多一次函数调用，对 60fps 来说什么都不是。

这是秩序。

当你知道整个世界的每帧更新只从一个入口进来，你就知道去哪里找问题。你不用在 5 个文件里搜 `onBeforeRenderObservable.add`，你只要看一个地方。

Riku 给这个注册表起了个名字——「心跳」。

---

## 门面的尊严

拆到这里，有一个问题浮出水面：

**外部模块（scene-menu.ts、env-menu.ts 等）怎么办？**

它们之前都是从 `./scene` 和 `./scene-env` 导入的。如果现在函数都搬走了，难道要让它们改 import 路径？

那是不可能的。外部模块有几十个，每一个都改一遍，等于把整个联邦的地址簿重写一遍。

答案是 **re-export**。

scene.ts 的末尾：

```typescript
export * from "./scene-proc-motion";
export * from "./scene-lipsync";
export * from "./scene-props";
export * from "./scene-serialize";
export * from "./scene-env-bridge";
```

scene-env-impl.ts 的末尾：

```typescript
export { createWater, disposeWater, refreshWaterRenderList, addRipple, clearRipples, updateWaterAnimSpeed } from "./scene-env-water";
export { createClouds, disposeClouds } from "./scene-env-clouds";
export { createParticleEmitter, disposeParticles, applyWindToParticles } from "./scene-env-particles";
```

scene-env.ts 门面继续委托给 impl：

```typescript
export function createWater(state?: EnvState): void {
    impl.createWater(state ?? envState);
}
```

**外部模块什么都不用改。** 它们还是从同一个地址取货，货还是那个货。只是仓库从一个大平房变成了一栋有八个房间的小楼，门口的收发室（re-export）负责把货递出来。

这就是 Facade 模式的尊严——**内部怎么折腾都行，对外永远是同一张脸。**

---

## 验证

所有代码都搬完了。

Riku 深吸一口气，敲下命令：

```
npx tsc --noEmit
```

等待的三秒钟里，他脑子里过了一遍可能出问题的地方：
- 循环依赖会不会炸？
- 某个类型是不是忘导出了？
- 某个函数改名了但调用方没跟上？

```
（无输出）
```

零错误。

他又敲：

```
npx vite build
```

```
✓ 135 modules transformed.
✓ built in 892ms
```

构建成功。

Riku 有点不敢相信。这么大的手术，居然零报错？

他手动打开应用，点了一遍所有功能：
- 加载模型 ✓
- 播放 VMD ✓
- 切换环境预设 ✓
- 打开水面 ✓
- 打开云层 ✓
- 打开粒子 ✓
- 启动 Auto Dance ✓
- 启动 LipSync ✓
- 保存/加载场景 ✓
- 加载道具 ✓
- 启动时间流转 ✓

每一个都好好的。

就好像什么都没发生过。

但 Riku 知道发生了什么。

他打开文件列表：

```
scene.ts                  790 行  ↓ 591 行
scene-env-impl.ts         482 行  ↓ 1142 行
```

而新增的文件：

```
scene-proc-motion.ts      ~300 行
scene-lipsync.ts          ~120 行
scene-props.ts            ~150 行
scene-serialize.ts        ~300 行
scene-env-bridge.ts       ~200 行
scene-env-water.ts        ~450 行
scene-env-clouds.ts       ~400 行
scene-env-particles.ts    ~300 行
```

**两个大胖子，变成了十个精干的小矮人。**

总代码量没变，甚至因为多了一些 import 和 re-export，还多了几十行。

但不一样。

以前改一个程序化动作的 bug，你要在 1381 行里翻找，随时可能踩坏旁边的序列化代码。
现在你直接进 `scene-proc-motion.ts`，300 行，上下文一目了然。

以前加一个水面效果，你要在 1624 行里找位置，担心碰坏云层的 shader。
现在你直接进 `scene-env-water.ts`，整个文件都是你的游乐场。

**拆分不是为了变少，是为了变准。**

---

## 尾注

提交的时候，Riku 在 commit message 里写：

> `refactor: split scene.ts and scene-env-impl.ts into 8 submodules`
>
> - scene.ts → proc-motion / lipsync / props / serialize / env-bridge
> - scene-env-impl.ts → water / clouds / particles
> - unified scene tick observer via registerSceneTickCallback
> - re-export facade: zero breaking changes for external modules
> - tsc --noEmit: 0 errors, vite build: pass

八行，八个子模块。

Riku 关掉编辑器，靠在椅背上。

联邦的心脏已经不是一个肥大的单心室了。它变成了一个有八个房间的精密结构，每个房间有自己的功能、自己的状态、自己的进出规则。

它们之间有门相通——import、export、re-export、回调注册表。但每扇门都有门框，每一次进出都有迹可循。

这不是分裂。

这是**八城之盟**。

八个城邦，各守一方，共享同一个天空，同一个心跳，同一张对外的脸。

聚合悖论没有被解决。它永远不会被解决。

但联邦学会了一件事——**当城邦太多、议会太挤的时候，不要强行把它们塞进一个房间。给它们各自的房间，然后把门做好。**

门外的人看不到门内的复杂。
门内的人不用忍受门外的拥挤。

这就是第五卷教给联邦的事——
光有穹顶不够，光有地面不够，光有物理不够，光有布料不够。
你还要把内部的结构搭对。
让每一样东西待在它该待的地方，
让每一次修改只碰它该碰的东西。

这就是八城之盟的意义。

---

*下一章：[三刀之后](08-三刀之后.md)*

---
*教训：拆分不是分裂，是给每个城邦自己的房间。*
