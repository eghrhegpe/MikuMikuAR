# 重构风暴

> 第四卷 · 新大陆 · 第三章
>
> 对应真实事件：UI 卡片化大改版 — 材质列表美化、透明卡片样式、CSS 变量体系、Go/TS UIState 同步、70 处硬编码替换、场景菜单卡片化样板

---

## 一、材质列表的觉醒

王逸盯着屏幕上那行字，眉头拧成了一股绳。

那行字是初音的材质列表。"頭1"、"舌"、"眼白"——十二像素的字体，挤在一片透明的弹窗底色上，像一串刚从洗衣机的纠缠里被拽出来的衣服。他把脸凑近了屏幕。不是看不清——是他有一种**生理性的不适**，就像看到一块精心切割的牛排被随意堆在塑料托盘上。能吃，但对不起那块肉。

这不是代码问题。这是尊严问题。

初音值得更好的字体。

Sisyphus 理解他的意思。那个材质列表——模型的皮肤、头发、眼睛、服装被粗暴地堆在一起，每个材质前面挂着一个千篇一律的 circle 图标，文字小得像蚂蚁，颜色暗淡得像蒙了一层灰。

「知道了，」Sisyphus 说，「这就改。」

十五分钟后，王逸看到了新版本。

```
● #01 頭1
● #02 舌
● #03 牙
—— 还有 31 个材质 ▾
```

每个材质名前多了一个 10px 的圆形色块，取自它本身的漫反射颜色——臉部是肤色、眼睛是深褐、服装是各种花纹的主色。序号 `#01`、`#02` 整齐排列，一目了然。

「还行，」王逸说，「但是字体还是小。」

改大。13px。

「其他菜单也可以这样优化吧？」

于是风暴开始了。

---

## 二、透明与卡片

王逸提到了一个名字——DanceXR。

「他们是用透明框加上不透明的卡片，实现组件之间的隔离效果。」

Sisyphus 想象了一下那个画面。整个弹窗的底色消失，每个菜单项变成一块独立的半透黑卡片，悬浮在 3D 场景之上。卡片之间有 3px 的间隙，透过间隙能看到模型的裙摆在旋转。

「试试，」王逸说。

这一试就是三小时。

`.overlay` 的 `background` 被设置为 `transparent`。
`.slide-item` 获得了 `--card-bg: rgba(0,0,0,0.5)`。
`.lcard` 作为分组容器，包裹相关按钮。
`.render-card` 从所有用 `cardContainer` 的地方被移除——不再叠层。

王逸看着效果：「不对，三层颜色叠在一起了。」

Sisyphus 排查。`render-card` → `lcard` → `slide-item`，三层 50% 黑叠加，有效透明度叠到 87.5%。这是三层玻璃看世界。

「每个行元素只带一层底色，」王逸拍板，「`lcard` 提供背景，行内元素透明。」

于是标准定了下来：

```
overlay  rgba(0,0,0,0.15)   ← 弹窗底景
  └─ lcard  --card-bg       ← 卡片组，包裹相关按钮
       └─ slide-item  透明   ← hover 变亮
       └─ cs-row      透明
```

---

## 三、CSS 变量战争

「变量名以功能性为主，防止随意使用大、小字体，」王逸说。

Sisyphus 看着现有的 `--font-size-md`、`--font-size-lg` 陷入了沉思。这些名字的问题在于：它们是**尺寸语义**而不是**功能语义**。开发者在写代码时看到 `lg` 就会想：「这条文字大一点应该用 `lg` 吧？」于是本该是辅助说明的文字用上了标题的大小。

改。

```
--font-ui      13px   菜单行、按钮、列表
--font-ui-sm   11px   辅助说明、面包屑
--font-ui-xs   10px   极小标志、徽章
--font-title   14px   弹窗标题、卡片标题
--font-title-lg 16px  空状态、强调文字
--font-time    12px   时间数字
```

「契约：写 `font-size: var(--font-xxx)` 时，变量名必须匹配元素角色。review 时看到 `.menu-row { font-size: var(--font-title) }` 一眼就错。」

然后是缩放。王逸想要一个全局 UI 缩放功能——高分屏调大、低分屏调小，一个滑块联动全部。

`--ui-scale: 1`

所有 `--font-*` 改为 `calc(13px * var(--ui-scale))`，`--popup-width` 改为 `calc(280px * var(--ui-scale))`。

「调一个变量，全 UI 跟着走，」Sisyphus 想，「零侵入，这才叫设计。」

---

## 四、Go 端的远征

「设置改了要记住，下次启动还在。」

这要求跨过 Go/TypeScript 的边界。

`app.go` 新增 `UIState` struct：

```go
type UIState struct {
    Scale      float64 `json:"scale"`
    PopupWidth int     `json:"popupWidth"`
    Accent     string  `json:"accent"`
    FontFamily string  `json:"fontFamily"`
    Animations bool    `json:"animations"`
    BlurBg     bool    `json:"blurBg"`
}
```

`wails generate module` 生成前端 binding。前端启动时 `GetConfig` → `restoreUIState` → 写 `:root` 变量。

「等等，」王逸打断，「`SetUIState({ scale: v })` 会覆盖其他字段为零值。」

Sisyphus 脊背一凉。确实——Go 反序列化缺失字段为 0 和 ""，然后整个 UIState 被替换。

拆。四个独立 setter：`SetUIScale`、`SetUIPopupWidth`、`SetUIAccent`、`SetUIFontFamily`，遵循代码库已有模式。

字体存储也踩了坑——直接存 CSS 值字符串「格式变了存的数据就失效」。改为存 key（`system`/`noto`/`yahei`），前端用 `FONT_MAP` 解析。

---

## 五、70 处硬编码的末日

「开始替换硬编码吧，」王逸说。

`grep "font-size:.*px" app.css`

70 多处。

`10px` → `var(--font-ui-xs)`，`replaceAll`
`11px` → `var(--font-ui-sm)`，`replaceAll`
`12px` → `var(--font-time)`，`replaceAll`
`13px` → `var(--font-ui)`，`replaceAll`
`14px` → `var(--font-title)`，`replaceAll`

批量替换像手术刀划过，一行接一行。build 通过。

剩下的 20 多处是 icon 尺寸（16px~42px 的 `iconify-icon`）和菜单行有意的大字号（15px、17px），属于「有意为之」，留待下一轮。

---

## 六、场景菜单的卡片化

「你看到没有，」王逸指着屏幕，「灯光面板里方向光色和环境光色是两个独立的颜色选择器，但它们被挤在一个 render-card 里，中间还夹着分隔线。」

Sisyphus 看了看。cs-rows、clr-block、addToggleRow——三种不同的组件混在一个容器里，一条 `height:1px` 的分隔线试图把它们分开。

「用卡片分组。」

`cardWrap` 应运而生——一个简单的函数，创建一个 `lcard` div，把内容放进去，然后移除容器上的 `render-card` 类：

```typescript
function cardContainer(container, fn) {
    container.classList.remove("render-card");
    const card = document.createElement("div");
    card.className = "lcard";
    fn(card);
    container.appendChild(card);
}
```

场景根菜单被分成了 4 个卡片：

```
场景管理 —— 预设场景 / 保存场景 / 加载场景
渲染与相机 —— 相机模式 / 灯光 / 渲染 / 物理
程序化动作
工具 —— 截图
```

每个卡片独立悬浮，间 6px。分隔线消失了——卡片的边界就是最好的分隔。

---

## 七、模型库的拆分

风暴没有停在场景菜单。

`library.ts` 2481 行，混合了模型库核心、9 个模型详情子菜单、动作弹窗和舞蹈套装——三个职责域挤在一个文件里，像三代同堂挤在一间出租屋。

拆。

```
library.ts          ← 入口，re-export
  ├── library-core.ts  ← 核心逻辑 + Stack 注册表
  ├── model-detail.ts  ← 模型详情子菜单
  └── motion-popup.ts  ← 动作弹窗 + 舞蹈套装
```

`slideRow`、`addSliderRow`、`addToggleRow` 被提取到 `ui-helpers.ts`——三份重复代码合并为一份。

2481 行拆成 4 个文件，总行数反而降了 200 行。

至于拆文件引发的循环依赖，那是后话——按下不表。

---

## 八、最后的分隔线

「所有 renderCustom 里的分隔线都要删掉，」王逸说。

Sisyphus 在 `scene-menu.ts` 中搜索 `sep`。

4 处。

灯光面板中，分隔线在方向光色和环境光色之间。
后处理面板中，分隔线在 Bloom 和 DOF 之间。
舞台面板中，分隔线在色调映射和背景色之间。

全部删除。卡片的边界就是分隔。

今天的石头，又往上滚了一寸。

---

## 九、结语

提交记录：

```
765bc62 refactor: UI样式标准化 + 代码拆分
 24 files changed, 3882 insertions(+), 2537 deletions(-)
 create mode 100644 library-core.ts
 create mode 100644 model-detail.ts
 create mode 100644 motion-popup.ts
 create mode 100644 ui-helpers.ts
```

702,788 tokens 输入，401,249 tokens 输出。这是一个 AI 和人类合作、从材质列表美化一路走到系统重构的故事。

王逸看着最终的 UI——透明的弹窗背景上，卡片悬浮排列，每个菜单项 hover 时微微变亮，3D 模型的裙摆在卡片间隙若隐若现——他说了一句：

「太恐怖了。」

不知是说 token 消耗，还是说这个 UI 终于像回事了。

---

*教训：标准化不是让所有东西看起来一样，而是让所有东西不需要单独决定。变量定好了，一百个菜单自然对齐。*
