# MikuMikuAR v1.5.3

## 新功能

### 无限地面（ADR-134）

- **ADR-134 Phase 1：无限地面** — `groundFollowCamera` 重命名为 `groundInfinite`，smooth-lerp 相机追踪跟随，世界空间 UV 补偿，支持远近端无限延伸过渡
- 恢复无限地面 UI toggle 开关（修复 stash 丢失）

### 动作系统

- **ADR-128：per-model 程序化动作状态** — `ModelInstance.procMotion` 独立管理，`_refProcState(modelId)` 按模型隔离，移除 `autoSwitch` 全局覆盖
- **模型详情卡片** — pinned 程序化动作引用 + loaded motion 点击恢复 + loadManager 集成
- **motion-popup 重构** — 动态 motion-module 导入，撤销/重做 UI，motion overlay 子面板

### 相机与库

- **相机预设** — camera-angle 更新 + motion pose levels 对齐
- **库浏览 UX 增强** — 动作/浏览/核心操作交互优化

### Babylon.js 升级

- **升级至 9.17.0** — `@babylonjs/core/loaders/materials` 从 9.16.1 → 9.17.0

### 修复

- `env infKey` 类型键 collateral 恢复
- bindings 重生成 + disposeProcMotion 修复 + menu-schema 测试适配
- 14 篇被误删小说章节恢复

### 其他

- 系统架构指南（`ads.txt`）新增目录结构
- E2E 测试加固

---

> 自 v1.5.2 以来 **11 次提交**，45 个文件变更，**+2694/−406 行**。
