# v1.6.5

## 新功能

### 浏览器端支持 (ADR-177)
- Phase 0-4 完整落地：Runtime Bridge 隔离层、browser-adapter 数据链、FSA 文件扫描、拖拽导入 (drop→IndexedDB→readFileBytes)
- GitHub Pages 部署上线 + Playwright @web 端到端 smoke 测试
- 能力门控 (capabilities) 驱动原生入口隐藏
- 网页端 zip 扫描展开内部文件、嵌套目录纹理关联、虚拟目录语义对齐桌面端

### 模型替换 (ADR-150)
- 替换模型继承当前状态 (captureInheritedState/applyInheritedState) + 撤销保护
- 触发 VMD apply after sceneMotionId inheritance

### 动作覆盖面板 (ADR-116)
- Phase 2 打磨：冲突文案用户语言化、骨骼搜索框、applyOverride 统一函数
- 高级骨骼覆盖自动保存闭环 + absolute 回填 + 清除确认 + 轴提示

### 渲染与画质
- 真 cel-shading 后处理 (posterize + Sobel) (ADR-076)
- 质量维度注册表 (ADR-174) + setEnvState 中间件化 (ADR-173)
- 金属舞台预设 (metalStage) + 粒子质量档位 (particleQuality)
- 降级系统接入 renderScale 像素比杠杆 + 移动端默认 balanced 档
- SDEF 球面变形真正生效

### 湿身效果 (ADR-172)
- 提取为独立模块 env-wetness，异常安全 + 纹理偏移全覆盖

### 前端 Backend 适配器 (ADR-176)
- 绞杀者模式双实现 (Web/Desktop 通杀)，43 个业务文件零改动完成全量路由

## 修复

- 渲染资源管理与状态一致性修复 (多处 P1-P3)
- 骨骼锁定关闭惯性避免与每帧 target 跟随冲突
- 触屏返回逻辑：安卓禁用坐标手势 + _isOpen 状态机 + iOS pan-y
- 刷新率基准上限 240 + 降级收敛 + resize 全量 recalc
- captureThumbnail toBlob 异步编码替代同步 toDataURL
- 横屏面板限高防止顶出屏幕
- 网页端 FSA 扫描/纹理关联/zip 加载/自动保存/持久化全链路修复
- i18n 补齐缺失 key、清理死 key、校准 ja/ko 术语

## 重构

- 合并 _getSlotFor/_getParamsFor/_getTintFor 为统一 _resolveVariant 泛型访问器
- 阵型菜单从场景菜单迁移至模型库
- 统一模型显示名为文件名，移除多名字优先级机制
- 消除 core→web-loader 循环依赖
- 4 个带 _ 前缀的 public 函数改名
- 灯光菜单 21 处重复 bind 统一

## 文档

- 知识库大规模补录（核心基础设施/场景编排/物理/环境渲染/菜单/场景子系统）
- ADR 状态同步 + 文档漂移脚本增强

## 统计

- 141 次提交 (自 v1.6.3)
- 558 文件变更
- +26827 / −5595 行
