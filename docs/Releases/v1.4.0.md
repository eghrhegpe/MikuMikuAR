# MikuMikuAR v1.4.0

## 新功能

### 环境系统全面重构

- **env-water 水流模拟系统**（ADR-084/115）
  - Babylon.js WaterMaterial 集成，CPU 法线纹理 + GPU 几何波动渲染
  - 48/16/6 三级 LOD 细分，距离阈值 30m / 80m 自适应切换
  - 平面反射（Planar Reflection）与环境贴图双路混合
- **env-lighting 光照系统**
  - 场景光照与环境预设（env_preset.go）完整接入
  - 灯光菜单声明式 Schema 重构，集成到统一环境面板
- **env-clouds 体积云着色器**
  - GLSL Ray Marching 实现（4 层 FBM + Beer-Lambert 衰减 + 边缘辉光）
  - 参数：云量 0.5、高度 325、缩放 0.55、厚度 15、可见距离 2000
  - 与场景光照联动，动态阴影投射
- **env-ground 地面系统**与 env-sky 天空分离
  - 独立地面跟随逻辑（ADR-085 部分实现）
  - 天空盒与体积云分层渲染

### 场景环境菜单重构（ADR-111）

- 统一环境面板：地面 / 天空 / 云 / 水 / 光照 / 粒子归一化入口
- 声明式 Schema 驱动，57 面板全部迁移完成

### 道具库菜单拆分（ADR-111）

- 拆分为 actions（操作）/ browse（浏览）/ setup（设置）三个子面板
- 列表模式项实时重算，解决切换视角后快照陈旧问题

### 角色行三区交互

- 左区 Radio 切换焦点角色
- 中区文本点击进入详情
- 右区齿轮打开工具菜单

### 状态栏与快捷键（ADR-100）

- 完整状态栏 UI（status-bar 模块）
- 快捷键系统（shortcut-app）
- 应用级事件总线（events）

### 动作系统修复

- **修复缩略图截帧时动画被清空**（vmd-loader）：`stopProcMotion` 延迟到 `vmdData` 赋值之后
- **修复动作叠加多余层**：添加动作时无条件替换基础动作
- **移除动作弹窗「最近使用」死代码**

### 国际化补全

- 5 个 locale 补全 10 个缺失翻译 key

---

## 架构改进

- **ADR-107** 流式音频播放器完成
- **ADR-108** 动画重定向器（Animation Retargeter）完成
- **ADR-112** SDEF Injector 完成
- **ADR-113** 地平线体积云落地
- **ADR-114** 地面反射增强完成
- **ADR-116** 骨骼覆盖 UI 重新设计

---

## Bug 修复

| 问题 | 修复 |
|------|------|
| 缩略图截帧时动画被清空 | `stopProcMotion` 延迟到 `vmdData` 赋值后 |
| 动作叠加多余层 | 添加动作时无条件替换基础动作 |
| 列表模式快照陈旧 | 实时重算列表项 |
| `autoExpand` 空层级误推 | 守卫 `allModels` 扫描完成前不推送 |
| 翻译 key 缺失 | 5 locale 补全 10 个 key |

---

## 性能与工程

- 场景模块全面重构（ground / texture / water / planar-reflection / props / type-helpers）
- Go 后端集成：`app.go`、`integration.go`、`library.go`、`scene.go`、`watch.go` 统一接入环境总线
- 前端 UI 组件：`ui-slide-row`、`vmd-layers`、`lipsync-bridge` 同步 i18n
- 配套文档：ADR 113–116 新增/更新，buglog 补充时序冲突条目

> 详情查阅各模块 ADR 与 `docs/adr/`。