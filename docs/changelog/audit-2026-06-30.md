# 前端全模块深度审计记录（第二轮）

> 🔍 2026-06-30 完成。覆盖 scene/、physics/、motion/、outfit/、menus/ 共 25+ 文件，修复 80+ 项问题。

## 关键修复亮点

| 模块 | 关键修复 |
|------|---------|
| `xpbd-solver.ts` | 子步内 Verlet 积分重构（外力移入子步循环）、地面碰撞增加弹性系数 |
| `xpbd-cloth.ts` | 锚点粒子 prevP 不覆盖（防撕裂）、碰撞胶囊矩阵每帧更新、移除 globalThis.BABYLON |
| `xpbd-collider.ts` | 摩擦改为 prevP 实现、多胶囊防重复摩擦、updateCapsuleSizes 骨骼名修复 |
| `scene/render/lighting.ts` | transitionLighting 跳帧保护 + shadowBias 实时生效 + 夜间逻辑条件修正 + setSkipLightAutoSave |
| `scene/env/env-bridge.ts` | 打断循环依赖（→ impl 直导）、预设动画省保存（skipAutoSave）、isFullRestore 移除、动画取消机制 |
| `scene/motion/lipsync-bridge.ts` | modelRegistry 注入 ModelManager、聚焦变化自动重置 morph 名 |
| `scene/motion/vmd-loader.ts` | 旧动画句柄释放 + wasmAnimation 泄漏修复 + VmdLoader 清理 |
| `scene/manager/material.ts` | instanceof StandardMaterial 守卫 + resetMatCatParams 同步清理 \_matState |
| `scene/manager/model-loader.ts` | 模型预捕获材质原始值 + 注册顺序修正 + 加载锁提示 + modelRegistry→\_modelManager |
| `motion/vmd-writer.ts` | trailer 补第四字段(ik count) + Shift-JIS 编码表 + 帧排序 + 字符边界截断 |
| `motion/beat-detector.ts` | GainNode 音量控制 + AudioContext try-catch + dispose 清理 phaseStartTime |
| `menus/menu.ts` | \_cancelAnim 动画取消 + \_pendingTimeouts 跟踪 + dispose() + keydown 清理 + async renderCustom |
| `menus/outfit-ui.ts` | loadOutfits try-catch + applyOutfitVariant await + 重置刷新 + 图标改用 createIconifyIcon |

## 审计时发现的高频模式（写新代码注意）

1. **SlideMenu 生命周期**：每次 `new SlideMenu()` 前必须 `oldMenu?.dispose()`，否则 keydown 监听器累积
2. **modelRegistry 来源**：优先用 `modelManager.modelRegistry`（非 `config.ts` 的裸 export），或注入 ModelManager
3. **异步操作缺 try-catch**：`loadVMDFromPath`、`applyOutfitVariant` 等均需 catch + setStatus
4. **`innerHTML` 拼接未转义**：优先用 DOM 方法（`textContent`/`createElement`）或 `escapeHtml`
5. **XPBD 物理**：碰撞胶囊必须每帧 `updateMatrices`，摩擦须改 `prevP`（非 `p.v`），子步须含外力积分
